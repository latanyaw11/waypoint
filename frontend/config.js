// Point this at your deployed backend (see /backend, deployed e.g. to Render).
// Local dev default assumes `npm run dev` running the backend on :4000.
window.WAYPOINT_CONFIG = {
  API_BASE_URL: "https://waypoint-api-7d0m.onrender.com",
};
